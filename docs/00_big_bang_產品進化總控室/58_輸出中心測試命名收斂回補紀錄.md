# 輸出中心測試命名收斂回補紀錄

日期：2026-06-22

## 背景

前一輪已移除 `_export_real_attachments_showcase` 舊入口，但部分測試仍直接呼叫 `_showcase_*` 相容 alias。這會讓後續 AI 以為正式 GUI helper 仍以 showcase 命名為主。

## 本次調整

只修改測試，不改產品流程：

- `tests/test_billing_panel_logic.py`
  - 將輸出中心相關測試函式名稱從 `showcase` 改為 `output_center`。
  - 將 helper 呼叫改為正式 `_output_center_*` 名稱。
  - 測試資料中的 `showcase_summary.json` 改為 `output_center_summary.json`。

## 驗收

已執行：

```powershell
python -m pytest -s -q tests/test_billing_panel_logic.py tests/test_output_center_ui_smoke.py
```

結果：

```text
42 passed
```

並確認：

```powershell
rg -n "_showcase_|showcase_" tests/test_billing_panel_logic.py tests/test_output_center_ui_smoke.py
```

結果：無匹配。

## 下一步

產品碼中仍保留 `_showcase_*` 相容 alias。下一步可在 full tests 保護下移除這批 alias，讓正式 GUI helper 名稱完全收斂到 `_output_center_*`。
