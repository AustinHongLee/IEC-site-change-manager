# 輸出範圍選擇回補紀錄

日期：2026-06-18

## 目的

把 `照片PDF` 從「永遠輸出全部 attachments」往正式輸出中心前進一步。

這次先完成最重要的範圍控制：

- 全部 attachments
- 目前篩選結果
- 選取修改單

## 資料層契約

### `control/canonical_report.py`

`collect_canonical_report_set()` 新增：

```python
include_report_keys=[("20260112", "0547_AG")]
```

若未提供，維持原本行為：收集全部 records + attachments。

若有提供，只收指定 `(date, folder)`。

### `control/real_attachments_showcase.py`

`run_real_attachments_showcase()` 新增同樣的 `include_report_keys` 參數，並在 summary 寫入：

- `scope.mode`
- `scope.requested_count`

### `tools/run_real_attachments_showcase.py`

CLI 新增：

```powershell
--include 20260112/0547_AG
```

可重複指定多筆。

## GUI 回補

### `control/gui_panels.py`

`照片PDF` 按下後會先開啟範圍選擇對話框：

- 全部 attachments
- 目前篩選結果
- 選取修改單

紀錄清單改成可多選，讓「選取修改單」可輸出一張或多張。

歸檔資料不會進入 showcase 輸出範圍。

## 測試

新增/擴充：

- `tests/test_billing_panel_logic.py`
- `tests/test_run_real_attachments_showcase_tool.py`

已跑 focused：

```powershell
python -m pytest -s `
  .\tests\test_billing_panel_logic.py `
  .\tests\test_run_real_attachments_showcase_tool.py `
  .\tests\test_canonical_report.py
```

結果：

- 35 passed

## 實資料驗證

用目前 attachments 實跑單筆輸出：

```powershell
python .\tools\run_real_attachments_showcase.py `
  --output .\staging\real_attachments_showcase_selected `
  --overwrite `
  --include 20260112/0547_AG `
  --json
```

結果：

- report_count: 1
- folder: `0547_AG`
- weld_count: 7
- material_row_count: 3
- photo_count: 4
- PDF: 2 份

確認沒有把 `55_2a2` 混入輸出。

## 下一步

輸出中心下一層應該做「輸出內容選擇」：

- 統計單 Excel
- summary PDF
- photo grid PDF
- 未來公司正式 PDF 表單

現在範圍已經能切，下一步才值得切輸出內容。
